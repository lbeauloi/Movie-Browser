import React from "react";

const SearchBar = function () {
    return(
        <input type="text" placeholder="        Search here..." /> //pour mettre la loupe search comme image, il faut la mettre en background via le scss
    );
    
};
export default SearchBar